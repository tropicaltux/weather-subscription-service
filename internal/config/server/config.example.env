# Weather Subscription Service Configuration
# Example environment variables

# Application environment: development or production
# Optional - defaults to production if not specified or invalid
APP_ENV=production

# Allowed host configuration - required in production
# Used for CORS allowed origins (https://{ALLOWED_HOST})
ALLOWED_HOST=weather.example.com

# Server port
PORT=3000